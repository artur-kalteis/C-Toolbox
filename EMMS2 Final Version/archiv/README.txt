#Hier liegen der C-Code der einzelnen ArangoDB und InfluxDB Parser.
#Kompiliert werden diese mit dem Befehl: gcc filename.c -o filename -lcurl -ljson-c 
#Die kompilierten Programme kommen in das Verzeichnis /lib